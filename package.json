{
  "name": "auth-service",
  "version": "1.0.0",
  "main": "dist/server.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "rm -rf dist && npx tsc -p tsconfig.json",
    "dev": "npx nodemon src/server.ts",
    "start": ""
  },
  "author": "Shivam Malhotra",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.7",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "fastify": "^4.25.2"
  }
}
